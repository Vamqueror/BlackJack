
class Card{
    constructor(value,type){
        this.value=value
        this.type=type
    }
    /*

    get value(){
        return this.value
    }

    set value(val){
        if(values.includes(val))
            this.value=val
    }

    get type(){
        return this.type
    }

    set type(typ){
        if(types.includes(typ))
            return this.type=typ
    }
    */
}

export default Card